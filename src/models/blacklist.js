export class Blacklist {
  get url () { return this._url }
  set url (url) { this._url = url }

  get defaultUrl () { return this._defaultUrl }
  set defaultUrl (defaultUrl) { this._defaultUrl = defaultUrl }
}
